# Optional API keys — the pipeline runs fine without them (sentiment features
# are simply skipped when a key is absent). Copy to `.env` and fill in to enable.
NEWSAPI_KEY=
ALPHAVANTAGE_KEY=

# MLflow tracking — SQLite backend (modern default; unlocks the model registry).
MLFLOW_TRACKING_URI=sqlite:///mlflow.db
