"""stockfc — ASX stock-price forecasting with a full MLOps lifecycle.

Sub-packages:
    data      — market-data ingestion (yfinance) with caching + offline fallback
    features  — pure-pandas technical-indicator feature engineering
    models    — ARIMA / XGBoost / LSTM training, evaluation and the model registry
"""

__version__ = "0.1.0"
